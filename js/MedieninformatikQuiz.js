import { initQuiz } from './quizBase.js';

const correctAnswers = {
  // TODO: Add correct answers for this quiz
};

document.addEventListener('DOMContentLoaded', () => {
  initQuiz('quiz-form', correctAnswers);
});
